/**
 * Global test setup file.
 * Loaded by Angular's build:unit-test runner after polyfills and TestBed initialization.
 * The jest-zone-shim.ts polyfill handles zone.js/testing ProxyZone setup for fakeAsync().
 */
